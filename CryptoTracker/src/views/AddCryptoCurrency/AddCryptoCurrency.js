import React from 'react';
import { Text, View } from 'react-native';

const AddCryptoCurrency = (props) => {
  return (
    <View style={{ backgroundColor: 'green' }}>
      <Text>Screen: AddCryptoCurrency</Text>
    </View>
  );
};

export default AddCryptoCurrency;
